import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  ratingContainer: {
    marginTop: 5,
    flexDirection: 'row',
    alignItems: 'center',
  },
  containerStyle: {
    marginRight: 15,
    paddingVertical: 20,
  },
});

export default styles;
